const populateThemeData = (state = {}, action) => {
    switch(action.type){
        case "FETCH_THEME_DATA":
            return state;
        default:
                return state;
    }
}

export default populateThemeData;